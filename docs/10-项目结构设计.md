# Online Judge 系统 - 项目结构设计

## 1. 整体项目结构

```
OnlineJudge/
├── README.md                    # 项目说明
├── LICENSE                      # 开源协议
├── .gitignore                   # Git 忽略文件
├── docker-compose.yml           # Docker Compose 配置
├── Makefile                     # 常用命令快捷方式
│
├── doc/                         # 📚 设计文档目录
│   ├── README.md
│   ├── 01-项目概述.md
│   ├── 02-系统架构设计.md
│   ├── ...
│   └── 10-项目结构设计.md
│
├── backend/                     # 🐍 后端项目 (FastAPI + uv)
│   ├── pyproject.toml          # Python 项目配置
│   ├── uv.lock                 # uv 锁文件
│   ├── requirements.txt        # 生产依赖
│   ├── requirements-dev.txt    # 开发依赖
│   ├── .env.example            # 环境变量示例
│   ├── main.py                 # FastAPI 应用入口
│   ├── alembic.ini             # Alembic 配置
│   ├── alembic/                # 数据库迁移
│   │   ├── env.py
│   │   └── versions/
│   ├── app/                    # 应用代码
│   │   ├── __init__.py
│   │   ├── main.py            # FastAPI app 实例
│   │   ├── core/              # 核心配置
│   │   │   ├── __init__.py
│   │   │   ├── config.py     # 配置管理
│   │   │   ├── database.py   # 数据库连接
│   │   │   ├── security.py   # 安全相关（JWT等）
│   │   │   └── deps.py       # 依赖注入
│   │   ├── api/               # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── v1/           # API v1
│   │   │   │   ├── __init__.py
│   │   │   │   ├── router.py # 总路由
│   │   │   │   ├── users.py
│   │   │   │   ├── problems.py
│   │   │   │   ├── submissions.py
│   │   │   │   ├── contests.py
│   │   │   │   └── discussions.py
│   │   │   └── deps.py       # API 依赖
│   │   ├── models/            # SQLAlchemy 模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── problem.py
│   │   │   ├── submission.py
│   │   │   ├── contest.py
│   │   │   └── discussion.py
│   │   ├── schemas/           # Pydantic 模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── problem.py
│   │   │   ├── submission.py
│   │   │   ├── contest.py
│   │   │   └── response.py   # 统一响应格式
│   │   ├── services/          # 业务逻辑层
│   │   │   ├── __init__.py
│   │   │   ├── user_service.py
│   │   │   ├── problem_service.py
│   │   │   ├── submission_service.py
│   │   │   ├── contest_service.py
│   │   │   └── judge_service.py
│   │   ├── utils/             # 工具函数
│   │   │   ├── __init__.py
│   │   │   ├── cache.py
│   │   │   ├── pagination.py
│   │   │   └── validators.py
│   │   └── middleware/        # 中间件
│   │       ├── __init__.py
│   │       ├── auth.py
│   │       └── logging.py
│   ├── tests/                  # 测试
│   │   ├── __init__.py
│   │   ├── conftest.py
│   │   ├── test_api/
│   │   └── test_services/
│   └── scripts/                # 脚本
│       └── init_db.py
│
├── frontend/                    # ⚛️ 前端项目 (React + bun)
│   ├── package.json            # 项目配置
│   ├── bun.lockb               # bun 锁文件
│   ├── tsconfig.json           # TypeScript 配置
│   ├── vite.config.ts          # Vite 配置
│   ├── tailwind.config.js      # TailwindCSS 配置
│   ├── .env.example            # 环境变量示例
│   ├── index.html              # HTML 入口
│   ├── public/                 # 静态资源
│   │   ├── favicon.ico
│   │   └── robots.txt
│   ├── src/                    # 源代码
│   │   ├── main.tsx           # 应用入口
│   │   ├── App.tsx            # 根组件
│   │   ├── pages/             # 页面组件
│   │   │   ├── Home/
│   │   │   ├── Problems/
│   │   │   ├── ProblemDetail/
│   │   │   ├── Submissions/
│   │   │   ├── Contests/
│   │   │   ├── User/
│   │   │   └── Admin/
│   │   ├── components/        # 通用组件
│   │   │   ├── Layout/
│   │   │   ├── CodeEditor/
│   │   │   ├── Navbar/
│   │   │   └── Footer/
│   │   ├── hooks/             # 自定义 Hooks
│   │   ├── services/          # API 服务
│   │   ├── stores/            # 状态管理
│   │   ├── utils/             # 工具函数
│   │   ├── types/             # TypeScript 类型
│   │   ├── styles/            # 全局样式
│   │   └── constants/         # 常量定义
│   └── tests/                  # 测试
│
├── judge-worker/                # ⚖️ 判题工作节点 (Python)
│   ├── pyproject.toml
│   ├── requirements.txt
│   ├── .env.example
│   ├── main.py                 # 主程序入口
│   ├── worker/                 # 判题核心
│   │   ├── __init__.py
│   │   ├── judge.py           # 判题主逻辑
│   │   ├── compiler.py        # 编译器
│   │   ├── runner.py          # 运行器
│   │   ├── comparator.py      # 输出对比
│   │   └── sandbox.py         # 沙箱管理
│   ├── languages/              # 语言配置
│   │   ├── __init__.py
│   │   ├── cpp.py
│   │   ├── python.py
│   │   ├── java.py
│   │   ├── go.py
│   │   └── javascript.py
│   ├── config/                 # 配置
│   │   ├── docker_config.py   # Docker 配置
│   │   ├── seccomp_profile.py # Seccomp 配置
│   │   └── language_config.py # 语言配置
│   ├── utils/                  # 工具
│   └── tests/                  # 测试
│
├── docker/                      # 🐳 Docker 配置
│   ├── backend/
│   │   ├── Dockerfile
│   │   └── entrypoint.sh
│   ├── frontend/
│   │   ├── Dockerfile
│   │   └── nginx.conf
│   ├── judge-worker/
│   │   └── Dockerfile
│   └── judge-images/           # 判题环境镜像
│       ├── cpp/
│       │   └── Dockerfile
│       ├── python/
│       │   └── Dockerfile
│       ├── java/
│       │   └── Dockerfile
│       ├── go/
│       │   └── Dockerfile
│       └── javascript/
│           └── Dockerfile
│
├── scripts/                     # 🔧 脚本工具
│   ├── setup.sh                # 环境初始化
│   ├── dev.sh                  # 启动开发环境
│   ├── build.sh                # 构建项目
│   ├── deploy.sh               # 部署脚本
│   └── seed-data.py            # 数据填充
│
├── nginx/                       # Nginx 配置
│   ├── nginx.conf
│   └── ssl/                    # SSL 证书
│
├── data/                        # 📁 数据目录 (gitignore)
│   ├── postgres/               # PostgreSQL 数据
│   ├── redis/                  # Redis 数据
│   ├── test-cases/             # 测试数据
│   └── uploads/                # 用户上传
│
└── logs/                        # 📝 日志目录 (gitignore)
    ├── backend/
    ├── judge-worker/
    └── nginx/
```

## 2. 后端项目详细结构（FastAPI）

### 2.1 FastAPI 应用结构

**核心概念：**
- `models/`: SQLAlchemy 数据模型（数据库表）
- `schemas/`: Pydantic 模型（API 请求/响应）
- `api/`: 路由和端点
- `services/`: 业务逻辑层
- `core/`: 核心配置和依赖

### 2.2 模块示例（以 problems 为例）

```python
# backend/app/models/problem.py
from sqlalchemy import Column, Integer, String, Text
from app.core.database import Base

class Problem(Base):
    __tablename__ = "problems"
    
    id = Column(Integer, primary_key=True)
    title = Column(String(200), nullable=False)
    description = Column(Text, nullable=False)
    # ...

# backend/app/schemas/problem.py
from pydantic import BaseModel

class ProblemBase(BaseModel):
    title: str
    description: str

class ProblemCreate(ProblemBase):
    pass

class ProblemResponse(ProblemBase):
    id: int
    
    class Config:
        from_attributes = True

# backend/app/api/v1/problems.py
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.deps import get_db
from app.services import problem_service
from app.schemas.problem import ProblemResponse

router = APIRouter()

@router.get("/", response_model=list[ProblemResponse])
async def get_problems(db: AsyncSession = Depends(get_db)):
    return await problem_service.get_all(db)

# backend/app/services/problem_service.py
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from app.models.problem import Problem

async def get_all(db: AsyncSession):
    result = await db.execute(select(Problem))
    return result.scalars().all()
```

### 2.3 配置文件结构

```python
# backend/app/core/config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # 应用配置
    APP_NAME: str = "Online Judge"
    DEBUG: bool = True
    
    # 数据库配置
    DATABASE_URL: str = "sqlite+aiosqlite:///./oj.db"
    
    # Redis 配置
    REDIS_URL: str = "redis://localhost:6379/0"
    
    # JWT 配置
    SECRET_KEY: str = "your-secret-key"
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    
    class Config:
        env_file = ".env"

settings = Settings()

# backend/app/core/database.py
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import sessionmaker, declarative_base

engine = create_async_engine(settings.DATABASE_URL, echo=settings.DEBUG)
AsyncSessionLocal = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
Base = declarative_base()

async def get_db():
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise

# backend/app/core/security.py
from datetime import datetime, timedelta
from jose import jwt
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
```

### 2.4 主应用文件

```python
# backend/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.api.v1.router import api_router
from app.core.config import settings

app = FastAPI(
    title=settings.APP_NAME,
    docs_url="/api/docs",
    redoc_url="/api/redoc",
    openapi_url="/api/openapi.json"
)

# CORS 配置
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
app.include_router(api_router, prefix="/api/v1")

@app.get("/")
async def root():
    return {"message": "Online Judge API"}

# backend/app/api/v1/router.py
from fastapi import APIRouter
from app.api.v1 import users, problems, submissions, contests

api_router = APIRouter()
api_router.include_router(users.router, prefix="/users", tags=["users"])
api_router.include_router(problems.router, prefix="/problems", tags=["problems"])
api_router.include_router(submissions.router, prefix="/submissions", tags=["submissions"])
api_router.include_router(contests.router, prefix="/contests", tags=["contests"])
```

## 3. 前端项目详细结构

### 3.1 页面组件结构（以 Problems 为例）

```
frontend/src/pages/Problems/
├── index.tsx                    # 页面入口
├── ProblemList.tsx             # 题目列表
├── ProblemCard.tsx             # 题目卡片
├── ProblemFilter.tsx           # 筛选器
├── useProblemList.ts           # 自定义 Hook
└── styles.module.css           # 样式文件（可选）
```

### 3.2 服务层结构

```typescript
// frontend/src/services/api.ts
// API 基础配置

// frontend/src/services/problem.ts
export const problemService = {
  getList: (params) => api.get('/problems', { params }),
  getDetail: (id) => api.get(`/problems/${id}`),
  submit: (id, data) => api.post(`/problems/${id}/submit`, data),
};

// frontend/src/services/user.ts
export const userService = {
  login: (data) => api.post('/user/login', data),
  register: (data) => api.post('/user/register', data),
  getProfile: () => api.get('/user/profile'),
};
```

### 3.3 状态管理结构

```typescript
// frontend/src/stores/auth.ts
import { create } from 'zustand';

interface AuthState {
  user: User | null;
  token: string | null;
  login: (token: string, user: User) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  token: localStorage.getItem('token'),
  login: (token, user) => {
    localStorage.setItem('token', token);
    set({ token, user });
  },
  logout: () => {
    localStorage.removeItem('token');
    set({ token: null, user: null });
  },
}));
```

### 3.4 类型定义结构

```typescript
// frontend/src/types/problem.ts
export interface Problem {
  id: number;
  title: string;
  difficulty: 'easy' | 'medium' | 'hard';
  description: string;
  timeLimit: number;
  memoryLimit: number;
  tags: Tag[];
  acceptanceRate: number;
}

// frontend/src/types/submission.ts
export interface Submission {
  id: number;
  problemId: number;
  userId: number;
  language: string;
  code: string;
  status: SubmissionStatus;
  timeUsed: number;
  memoryUsed: number;
  createdAt: string;
}
```

## 4. 判题系统详细结构

### 4.1 核心模块

```python
# judge-worker/worker/judge.py
class Judge:
    """判题主类"""
    def __init__(self):
        self.docker_client = docker.from_env()
    
    def judge_submission(self, submission_id, problem_id, language, code, test_cases):
        """执行判题"""
        pass

# judge-worker/worker/sandbox.py
class Sandbox:
    """沙箱管理"""
    def create_container(self, config):
        """创建容器"""
        pass
    
    def run_code(self, container, input_data):
        """运行代码"""
        pass
```

### 4.2 语言配置

```python
# judge-worker/languages/cpp.py
CPP_CONFIG = {
    'compile_command': 'g++ {src} -o {exe} -O2 -std=c++17 -lm -Wall',
    'run_command': './{exe}',
    'src_name': 'main.cpp',
    'exe_name': 'main',
    'time_multiplier': 1.0,
    'memory_multiplier': 1.0,
}

# judge-worker/languages/python.py
PYTHON_CONFIG = {
    'compile_command': None,
    'run_command': 'python3 {src}',
    'src_name': 'main.py',
    'exe_name': None,
    'time_multiplier': 3.0,
    'memory_multiplier': 1.0,
}
```

## 5. Docker 配置结构

### 5.1 docker-compose.yml 结构

```yaml
version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:15
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: oj
      POSTGRES_USER: oj
      POSTGRES_PASSWORD: ${DB_PASSWORD}
  
  # Redis 缓存
  redis:
    image: redis:7-alpine
    volumes:
      - ./data/redis:/data
  
  # 后端服务
  backend:
    build: ./docker/backend
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.development
  
  # 前端服务
  frontend:
    build: ./docker/frontend
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
  
  # 判题工作节点
  judge-worker:
    build: ./docker/judge-worker
    volumes:
      - ./judge-worker:/app
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
  
  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./frontend/dist:/usr/share/nginx/html
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
```

## 6. 配置文件示例

### 6.1 后端环境变量 (.env)

```bash
# Django 配置
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库配置
DB_ENGINE=django.db.backends.postgresql
DB_NAME=oj
DB_USER=oj
DB_PASSWORD=your-db-password
DB_HOST=postgres
DB_PORT=5432

# Redis 配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Celery 配置
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# JWT 配置
JWT_SECRET_KEY=your-jwt-secret
JWT_EXPIRATION_DELTA=86400

# 判题配置
JUDGE_QUEUE_NAME=judge:queue
MAX_JUDGE_WORKERS=4
```

### 6.2 前端环境变量 (.env)

```bash
# API 配置
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_BASE_URL=ws://localhost:8000/ws

# 应用配置
VITE_APP_NAME=Online Judge
VITE_APP_VERSION=0.1.0

# 功能开关
VITE_ENABLE_MOCK=false
VITE_ENABLE_DEBUG=true
```

## 7. Makefile 快捷命令

```makefile
.PHONY: help setup dev build test clean

help:  ## 显示帮助信息
	@echo "可用命令："
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | awk 'BEGIN {FS = ":.*?## "}; {printf "  \033[36m%-15s\033[0m %s\n", $$1, $$2}'

setup:  ## 初始化开发环境
	@echo "安装 uv..."
	curl -LsSf https://astral.sh/uv/install.sh | sh
	@echo "安装 bun..."
	curl -fsSL https://bun.sh/install | bash
	@echo "设置后端环境..."
	cd backend && uv venv && uv pip install -r requirements.txt
	@echo "设置前端环境..."
	cd frontend && bun install
	@echo "设置判题系统..."
	cd judge-worker && uv venv && uv pip install -r requirements.txt

dev:  ## 启动开发环境
	docker-compose up -d postgres redis
	@echo "启动后端..."
	cd backend && python manage.py runserver &
	@echo "启动前端..."
	cd frontend && bun run dev &
	@echo "启动判题工作节点..."
	cd judge-worker && python main.py &

build:  ## 构建 Docker 镜像
	docker-compose build

test:  ## 运行测试
	cd backend && pytest
	cd frontend && bun test
	cd judge-worker && pytest

clean:  ## 清理临时文件
	find . -type d -name "__pycache__" -exec rm -rf {} +
	find . -type f -name "*.pyc" -delete
	rm -rf backend/.venv frontend/node_modules judge-worker/.venv

migrate:  ## 运行数据库迁移
	cd backend && python manage.py migrate

seed:  ## 填充测试数据
	cd backend && python manage.py loaddata fixtures/initial_data.json
```

## 8. 项目初始化步骤

### 8.1 克隆项目后的初始化

```bash
# 1. 克隆项目
git clone <repo-url>
cd OnlineJudge

# 2. 复制环境变量文件
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
cp judge-worker/.env.example judge-worker/.env

# 3. 编辑环境变量（修改密码等）
vim backend/.env

# 4. 运行初始化脚本
make setup

# 5. 启动数据库
docker-compose up -d postgres redis

# 6. 运行数据库迁移
cd backend
python manage.py migrate

# 7. 创建超级用户
python manage.py createsuperuser

# 8. 启动开发服务器
make dev
```

### 8.2 访问应用

- 前端：http://localhost:3000
- 后端 API：http://localhost:8000/api/v1
- Django Admin：http://localhost:8000/admin
- API 文档：http://localhost:8000/api/docs

## 9. 开发工作流

### 9.1 日常开发流程

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 更新依赖
cd backend && uv pip install -r requirements.txt
cd frontend && bun install

# 3. 运行数据库迁移
cd backend && python manage.py migrate

# 4. 启动开发环境
make dev

# 5. 开发...

# 6. 运行测试
make test

# 7. 提交代码
git add .
git commit -m "feat: add new feature"
git push origin feature-branch
```

### 9.2 添加新功能

```bash
# 1. 创建新分支
git checkout -b feature/new-feature

# 2. 后端：创建新应用
cd backend
python manage.py startapp new_app
mv new_app apps/

# 3. 前端：创建新页面
cd frontend/src/pages
mkdir NewPage
touch NewPage/index.tsx

# 4. 开发...

# 5. 测试
make test

# 6. 提交
git commit -am "feat: add new feature"
git push origin feature/new-feature
```

## 10. 总结

这个项目结构设计遵循以下原则：

- ✅ **模块化**：前后端分离，功能模块独立
- ✅ **可维护**：清晰的目录结构，统一的命名规范
- ✅ **可扩展**：预留扩展空间，支持微服务拆分
- ✅ **标准化**：遵循 Django、React 最佳实践
- ✅ **自动化**：Makefile 简化常用操作

接下来我们将基于这个结构开始创建实际的项目文件！
